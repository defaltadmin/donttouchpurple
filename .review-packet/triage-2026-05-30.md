# Multi-AI Audit Triage — 2026-05-30

## Summary by Module

| Module | Critical | High | Medium | Low | Status |
|--------|----------|------|--------|-----|--------|
| engine/ | 1 | 1 | 4 | 8 | Critical+3 Medium fixed |
| services/ + workers/ | 0 | 0 | 1 | 1 | SEC-001 resolved, production-ready |
| components/ | 0 | 2 | 6 | 9 | 2 High fixed, 6 Medium fixed |
| config/ + utils/ + hooks/ + contexts/ | 1 | 1 | 6 | 7 | Critical+High fixed, 4 Medium fixed |
| styles/ | 0 | 0 | 0 | 3 | 3 Low fixed |
| **TOTAL** | **2** | **4** | **17** | **28** | |

## Fixed This Session (17 fixes)

### Critical (2)
1. **Evolve mode death at 0.5 health** — `health < 1` kills player at half-health. Changed to `health <= 0` in all 3 damage paths (GameEngine, TickProcessor miss, TickProcessor bomb).
2. **loginClaimedToday always false** — Date format mismatch. Standardized all `toDateString()` to `toISOString().slice(0,10)`.

### High (3)
3. **LottiePlayer onComplete re-initialization** — Stored in ref, removed from useCallback deps.
4. **ShareModal aria-hidden** — Removed `aria-hidden="true"` from backdrop (blocking screen readers).
5. **state-guard HMAC timing attack** — Switched from sign+string-compare to `crypto.subtle.verify()`.

### Medium (8)
6. **Medpack overcap** — Added `Math.min(GAME.MAX_HEARTS, ...)` after heal.
7. **Bomb in-place mutation** — `ref.active.push(bomb)` → `ref.active = [...ref.active, bomb]`.
8. **holdProgress dead field** — Replaced with correct `holdRequired` + `spawnedAt`.
9. **DustContext addDust race** — Fixed stale `next` variable; ref updated inside setState callback.
10. **DustContext spendDust race** — Added synchronous pre-check on dustRef before setState.
11. **DustContext double loadInitialDust** — Computed once, shared for useState+useRef.
12. **FPS key "F" in input fields** — Added INPUT/TEXTAREA/contentEditable guard.
13. **spendEnergy double-spend** — Moved guard inside setEnergyData callback.

### Low (4)
14. **CSS debug red outline in production** — Guarded with `body[data-dev="true"]` selector.
15. **Dead .toast animation in game.css** — Removed (overridden by enhancements.css).
16. **Dead rareSplashText keyframes in game.css** — Removed (overridden by enhancements.css).
17. **performance.css debug outline** — Production-guarded (same as #14).

## Not Fixed (accepted/deferred)

### Accepted
- SEC-004 App Check: Already re-enabled in firebase.ts:101-114
- Firebase config hardcoded: By design (client-side identifiers)
- Sentry auth token in .env.example: Accepted
- Bot superhuman at high accuracy: Accepted design
- i18n English only: Accepted
- Fixed interval tick: Accepted design
- Sourcemaps deployed: Accepted

### Deferred (Low severity, not worth fixing now)
- QuickSettings reactivity (LOW — toggles work via re-render from parent)
- GameContext memoization (LOW — context consumers already optimized with React.memo)
- BorderGlow onReady in deps (LOW — only fires on mount)
- ScoreFloat/GameOver hardcoded hex colors (LOW — cosmetic)
- PlayerPanel extra wrapper div (LOW — DOM count)
- ElasticSlider RAF loop (LOW — works correctly)
- PauseOverlay setInterval (LOW — runs once per second)
- NameChangeForm touch detection (LOW — autoFocus only)
- PwrBar RAF loop (LOW — works correctly)
- RewardsHub variable shadowing (LOW — cosmetic)
- PwrBar magic numbers (LOW — matches balance config)
- localStorage raw calls (LOW — 5 locations, most have try/catch)
- useGameSettings sync-on-mount (LOW — works via direct callback updates)
- achievements.ts raw getItem (LOW — falls to catch)
- BotController uses setInterval (LOW — has document.hidden check)
- Classic mode _slotsCache miss (LOW — 3x3 grid, cache not needed)
- _cachedNow not used by tap handlers (LOW — Date.now() correct between ticks)
- Duplicate _slotsCache WeakMaps (LOW — different value types, no conflict)
- DDA reaction time mixing taps (LOW — inflates reaction time, makes game easier)
- shieldBoss score window miss (LOW — may miss trigger on large bonus jumps)
- _sessionStartTime double init (LOW — field initializer dead code)
- _applySettings ignores liteMode (LOW — not wired up yet)
- bomb delta timer stale pattern (LOW — edge case, unlikely in practice)
